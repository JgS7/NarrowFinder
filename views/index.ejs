<!DOCTYPE html>
<html>

  <% include header %>

  <body>
    <div id = "container" style="width:100%;">

      <% include sidebar %>

      <div id="map"></div>

      <script src="/javascripts/mostrar_ocultar.js"></script>
      <script src="/javascripts/error_handling.js"></script>
      <script src="/javascripts/adaptadores.js"></script>
      <script src="/javascripts/mapa.js"></script>
        
      <script>
        var error_bbox = "<%= error_bbox %>"
        var error_ancho = "<%= error_Ancho %>"

        if (error_bbox){
          var errox_box = error_box(error_bbox);
        }

        if (error_ancho){
          var errox_ancho = error_a(error_ancho);
        }

        var b =  "<%= resu %>";
        //console.log(b);
        if (b.includes("{&#34;")){
          var bb = adaptRES(b);
          var re = adaptRES2(bb);
          console.log(dibujar(JSON.parse(re))); //devuelve true, que es el resultado de la funcion.
        }
        else {
          dibujar(b);
          console.log("Resu drawed");
        }

        var vias = "<%= calles %>";
        //console.log(vias);
        if (vias){
          add_info(vias);
          console.log("Text added")
        }
      </script>    
    </div>
  </body>
</html>
